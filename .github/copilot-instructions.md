# Copilot Instructions

## Project Guidelines
- XAML files in this MAUI workspace must be saved with a UTF-8 BOM, otherwise the MAUI XAML compiler fails with 'XLS0308: XML document must contain a root level element.'